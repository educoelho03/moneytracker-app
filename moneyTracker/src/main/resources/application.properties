spring.application.name=moneyTracker

# postgres Configuration
spring.datasource.url=jdbc:postgresql://localhost:${DB_PORT:5432}/moneyTracker
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:PASSWORD}

# JPA/Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Security
api.security.token.secret=${JWT_SECRET_KEY:my-secret-key}

# Mail Configuration
spring.mail.host=smtp.sendgrid.net
spring.mail.port=587
spring.mail.username=apikey
sendgrid.apiKey=${SENDGRID_API_KEY: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}
moneytracker.email.sender=no-reply-moneytracker@outlook.com

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration